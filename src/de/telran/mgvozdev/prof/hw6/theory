1. Что такое Set/List/Queue и для чего они нужны? это интерфейсы или классы?
    Интерфейсы, которые определяют разные виды коллекций для хранения и обработки данных

2. Что такое ArrayList?
    Одна из реализаций интерфейса List(наиболее используемая). По сути динамический массив, который изменяет свой размер
    при добавлении или удалении элементов

3. Что гарантирует Set? Что не гарантирует?
    Гарантирует уникальность элементов, но не гарантирует порядок сохранения элементов

4. Что не гарантирует List? Что гарантирует?
    Гарантирует порядок сохранения элементов и наличие дубликатов, но не гарантирует уникальность

5. Что стоит во главе иерархии Java Collection Framework?
    Во главе иерархии Java Collection Framework находится интерфейс java.util.Collection. Этот интерфейс объединяет
    общие методы и функциональность для всех коллекций в Java.

6. Какой вспомогательный класс есть для работы с массивами?
    java.util.Arrays

7. Можно ли написать следующую конструкцию? Collection<int> col = new ArrayList<>();
    Нет, нельзя использовать примитивные типы в качестве параметра в коллекциях
