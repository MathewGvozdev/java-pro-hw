1. Что является объектом в java?
    В java все является объектом (кроме примитивных типов). Представляет собой экземпляр какого либо класса со своими
    полями и поведением.

2. Как определяется какой метод будет вызван в случае передачи туда примитива? объекта?
    Зависит от сигнатуры метода, т.е. аргументов функции

3. Что такое autoboxing и unboxing?
    Преобразование между примитивными типами данных и их обертками (например, int -> Integer и наоборот). По сути
    синтаксический сахар, для удобства работы

4. Почему не стоить сравнивать через ==
    При сравнении объектов оператор == сравнивает ссылки в памяти, а не их значения

5. Сколько весит в памяти ссылка?
    8 байт

6. Почему объекты называются ссылочными типами данных?
    Потому что переменные, которые хранят объекты, хранят ссылки на объекты в памяти. Ссылка - адрес памяти,
    где хранится объект