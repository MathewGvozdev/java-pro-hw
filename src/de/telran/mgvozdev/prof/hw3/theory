1. Что такое интерфейс?
    Интерфейс - контракт, который определяет методы, которые должен реализовать класс, если имплементирует этот интерфейс

2. Что такое сигнатура метода?
    В сигнатуру метода входит название метода и параметры (именно типы:
    method1(int val, double fract);
    method2(double val, int fract);
    - разные методы)

3. Может ли быть 2 метода с одинаковой сигнатурой внутри одного класса?
    Нет

4. В чем разница между переопределением (Override) и перегрузкой(Overload)?
    Override используется для переопределения реализации метода, например, в потомках супер-класса. Вызывается в runtime,
    т.е. во время выполнения программы.
    Overload это перегрузка. Во время компиляции программы она определяет какой из перегруженных методов будет использоваться.
    В принципе две абсолютно разные концепции. При Override сигнатура методов одинаковая, а при Overload - разная.

5. Можно ли имплементировать несколько интерфейсов?
    Да

6. В чем отличие интерфейса от абстрактного класса?
    Интерфейс определяет поведение и не хранит никакого состояния, абстрактный класс же хранит его в виде переменных.
    У интерфейса нет конструкторов, имеет только абстрактные методы, все поля являются public static final.

7. Что такое enum?
    Тип данных, который предоставляет перечисления в виде постоянных значений

8. Какое правило наименование переменных в enum?
    Капсом как и константы (public static final). Например, MONDAY, TUESDAY ...

9. Можно ли унаследоваться от какого-то класса в enum? что по поводу interface?
    Нельзя наследовать от классов, можно реализовывать интерфейсы